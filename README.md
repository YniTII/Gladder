# 🔌 Ladder Simulator - Игровая Симуляция Релейной Логики

Интерактивный веб-симулятор релейных логических схем (Ladder Diagram, IEC 61131-3) с игровой механикой конвейера.

## 🚀 Быстрый старт

### Размещение на GitHub Pages

1. **Создайте репозиторий** на GitHub (например, `ladder-simulator`)
2. **Загрузите файл** `index.html` в корень репозитория
3. **Включите GitHub Pages**:
   - Перейдите в Settings → Pages
   - В разделе "Source" выберите ветку `main` (или `master`)
   - Сохраните настройки
4. **Доступ к приложению**: через несколько минут ваш симулятор будет доступен по адресу:
   ```
   https://<ваш-username>.github.io/<имя-репозитория>/
   ```

### Локальный запуск

Просто откройте файл `index.html` в любом современном браузере:
```bash
# macOS
open index.html

# Linux
xdg-open index.html

# Windows
start index.html
```

Или используйте локальный сервер:
```bash
# Python 3
python -m http.server 8000

# Node.js (если установлен http-server)
npx http-server -p 8000
```

## 📖 Инструкция по использованию

### Основы работы с LD-редактором

1. **Добавление элементов**: Кликните по элементу в палитре → он добавится на первую ступень
2. **Настройка элемента**: Двойной клик по элементу открывает окно конфигурации
3. **Удаление элемента**: Ctrl+Клик по элементу
4. **Новая ступень**: Клик по пустой области редактора
5. **Ручное управление**: Клик по контакту с типом "Ручное управление" переключает его состояние

### Типы элементов

| Элемент | Описание | Поведение |
|---------|----------|-----------|
| НО Контакт | Нормально открытый контакт | Пропускает ток, когда замкнут (state=true) |
| НЗ Контакт | Нормально закрытый контакт | Пропускает ток, когда разомкнут (state=false) |
| Катушка | Выходной элемент | Активируется при наличии тока, управляет выходами |
| Ветка ↑ | Начало параллельной ветви | Создаёт точку ветвления для логики ИЛИ |
| Ветка ↓ | Конец параллельной ветви | Объединяет ветви обратно |

### Типы привязки элементов

- **Ручное управление** — переключение кликом по элементу
- **Кнопка игры** — связь с кнопками ПУСК/СТОП на игровом поле
- **Датчик конвейера** — активация при прохождении заготовки
- **Выход (катушка)** — управление конвейером или другими устройствами

### Игровая симуляция

1. Нажмите **"▶ Запуск"** для старта симуляции
2. Используйте кнопки **ПУСК/СТОП** на игровом поле
3. Настройте LD-схему для автоматического управления:
   - Подключите кнопку ПУСК к НО контакту
   - Подключите кнопку СТОП к НЗ контакту (для безопасности)
   - Подключите катушку к выходу "Конвейер"

### Пример базовой схемы "Пуск-Стоп"

```
+24V --[НО Пуск]--[/НЗ Стоп]--(Конвейер)-- GND
                    |
                    |--[НО Конвейер]--|  (самоподхват)
```

**Логика работы**:
1. Нажатие ПУСК замыкает цепь → конвейер включается
2. Параллельный контакт Конвейер создаёт самоподхват
3. Нажатие СТОП размыкает цепь → конвейер выключается

## 🛠 Расширение функциональности

### Добавление новых элементов

Откройте `index.html` и найдите объект `LDEditor`. Добавьте новый тип элемента:

```javascript
// В getElementSVG добавьте SVG для нового типа
'new-element': `<svg viewBox="0 0 100 100">...</svg>`

// В palette добавьте HTML:
<div class="palette-item" data-element="new-element">
    <svg>...</svg>
    <span>Название</span>
</div>
```

### Настройка параметров конвейера

Найдите объект `GameEngine` и измените параметры:

```javascript
const conveyorSpeed = 0.2; // пикселей за миллисекунду
AppState.game.spawnInterval = 2000; // мс между спавном заготовок
```

### Добавление таймеров и счётчиков

Для расширения логики добавьте новые типы элементов в `LDEvaluator`:

```javascript
if (elem.type === 'timer') {
    // Логика таймера с задержкой
}
if (elem.type === 'counter') {
    // Логика подсчёта событий
}
```

## 📱 Адаптивность

Приложение полностью адаптировано для мобильных устройств:

- **Mobile-first** подход с базовыми стилями для маленьких экранов
- **Touch-события** для кнопок и элементов управления
- **CSS Grid и Flexbox** для адаптивной компоновки
- **Media queries** для экранов от 320px до 1920px+
- **Оптимизированные размеры** элементов на разных разрешениях

## ⚡ Производительность

- **requestAnimationFrame** для плавной анимации (60 FPS)
- **Фиксированный шаг логики** 50ms для стабильности
- **Минимум DOM-перерисовок** через точечное обновление классов
- **CSS-трансформации** вместо изменения positioning для анимаций

## 🐛 Отладка и тестирование

### Консоль разработчика

Откройте DevTools (F12) для просмотра логов:
```javascript
console.log('Текущее состояние:', AppState);
console.log('Входы:', AppState.inputs);
console.log('Выходы:', AppState.outputs);
```

### Тестирование на мобильных

1. **Chrome DevTools Device Mode**: F12 → иконка телефона
2. **Реальное устройство**: 
   - Задеплойте на GitHub Pages
   - Откройте ссылку на телефоне
3. **Проверка тачей**: Убедитесь, что кнопки реагируют на нажатия

### Проверка логики

1. Создайте простую схему: НО контакт → Катушка
2. Назначьте контакту "Ручное управление"
3. Кликните по контакту → катушка должна активироваться
4. Визуальная подсветка покажет путь тока

## 📄 Структура кода

```
index.html
├── <style>           # Все CSS стили
│   ├── Переменные CSS
│   ├── Базовые стили
│   ├── Компоненты (header, panel, game-area)
│   └── Media queries
└── <script>          # Вся логика на vanilla JS
    ├── AppState      # Единый объект состояния
    ├── LDEditor      # Рендер и управление LD-схемой
    ├── GameEngine    # Конвейер, заготовки, анимация
    ├── LDEvaluator   # Вычисление логики схемы
    ├── Integrator    # Связь LD и игры, цикл симуляции
    └── UIController  # Обработчики кнопок UI
```

## 🔒 Технические ограничения

- Только статические файлы (HTML, CSS, JS)
- Без серверной части
- Без фреймворков сборки
- Чистый JavaScript ES6+
- Нет внешних зависимостей

## 🎯 Возможные улучшения

- [ ] Сохранение схем в localStorage
- [ ] Экспорт/импорт схем в JSON
- [ ] Таймеры и счётчики (TON, TOF, CTU)
- [ ] Больше типов контактов (импульсные, с задержкой)
- [ ] Несколько конвейеров
- [ ] Сенсоры разного типа (индуктивные, оптические)
- [ ] Звуковые эффекты

## 📝 Лицензия

MIT License — свободное использование и модификация.

---

**Автор**: Senior Frontend Developer  
**Версия**: 1.0.0  
**Дата создания**: 2024Gladder